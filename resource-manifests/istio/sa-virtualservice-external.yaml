apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: mongodb-external-services
spec:
  hosts:
  - "*"
  gateways:
  - http-gateway                      # 1
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: mongodb
        port:
          number: 80